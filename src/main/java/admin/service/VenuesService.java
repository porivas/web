package admin.service;

import admin.model.Venues;
import admin.model.Venuestype;
import common.core.generic.GenericService;

/**
 * 业务接口
 *
 **/
public interface VenuesService extends GenericService<Venues, Integer> {
    /**
     * 通过用户id 查询用户 拥有的角色
     * 
     * @param userId
     * @return
     */
}
