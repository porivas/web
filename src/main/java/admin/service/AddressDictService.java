package admin.service;

import admin.model.Address;
import admin.model.Addressdict;
import common.core.generic.GenericService;

/**
 * 业务接口
 *
 **/
public interface AddressDictService extends GenericService<Addressdict, Integer> {
    /**
     * 通过用户id 查询用户 拥有的角色
     * 
     * @param userId
     * @return
     */
}
