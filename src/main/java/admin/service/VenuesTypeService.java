package admin.service;

import admin.model.Role;
import admin.model.Venuestype;
import common.core.generic.GenericService;

import java.util.List;

/**
 * 业务接口
 *
 **/
public interface VenuesTypeService extends GenericService<Venuestype, Integer> {
    /**
     * 通过用户id 查询用户 拥有的角色
     * 
     * @param userId
     * @return
     */
}
